{% extends 'base.html.twig' %}

{% block title %}Sistema de Control de Asistencia{% endblock %}
{% block stylesheets %}
    {{ parent() }}
    <link href="{{ asset('/ControlAsistencia/public/css/generales.css') }}" type="text/css" rel="stylesheet" />
{% endblock %}
{% block body %}

<div class="example-wrapper">
    <h1>Bienvenido al sistema de control de asistencia ✅</h1>
    <p>Para continuar deberá ingresar con su usuario y clave. Si no posee una cuenta de usuario comuniquese con el
        administrador del sistema.</p>
    <div class="container">
        <div class="row">
            <div class="offset-3 col-sm-4 col-md-4 col-lg-4">
                <div class="d-grid gap-2">
                    <a href="{{ path('app_login') }}" class="btn btn-lg btn-primary">Inicio de sesión</a>
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}
