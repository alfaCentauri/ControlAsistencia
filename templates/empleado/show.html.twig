{% extends 'base.html.twig' %}

{% block title %}Empleado{% endblock %}

{% block body %}
    <h1>Empleado</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ empleado.id }}</td>
            </tr>
            <tr>
                <th>Cedula</th>
                <td>{{ empleado.cedula }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ empleado.nombre }}</td>
            </tr>
            <tr>
                <th>Apellido</th>
                <td>{{ empleado.apellido }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('empleado_index') }}">Regresar</a>

    <a href="{{ path('empleado_edit', {'id': empleado.id}) }}">Editar</a>

    {{ include('empleado/_delete_form.html.twig') }}
{% endblock %}
